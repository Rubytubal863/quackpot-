# notify-send duck parade

fires a few desktop notifications, then they expire.

## needs

`notify-send` (libnotify) and a running notification daemon (most desktops have one).

## revert

none, the notifications time out on their own.
