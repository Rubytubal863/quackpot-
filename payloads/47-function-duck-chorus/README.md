# function duck chorus

opens `nano` on an empty buffer and types a verse/chorus duck song built from two
functions.

## needs

- linux/x11 with a terminal on `ctrl+alt+t` (swap it if your desktop differs).
- `nano` on `PATH`.

## revert

nothing is written to disk. to leave nano: ctrl+x, then n (don't save).
