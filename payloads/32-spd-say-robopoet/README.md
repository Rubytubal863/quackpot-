# spd-say robo-poet

reads a short robotic ode out loud.

## needs

`spd-say` (speech-dispatcher). the payload falls back to `espeak` if spd-say isn't
there; install one or nothing plays.

## revert

none, it's audio.
