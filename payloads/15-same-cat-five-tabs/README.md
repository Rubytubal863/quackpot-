# five tabs, one cat

opens the same page in five tabs.

the url is in the payload (`https://cat-bounce.com`): typed once to open the browser,
then again in a loop for four more tabs. swap it for any sfw site. change the `WHILE`
bound for more or fewer tabs (keep it gentle).

## revert

close the tabs.
