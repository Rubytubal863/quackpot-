# cowsay fortune duck

a cowsay duck reads a fortune.

## needs

best with `cowsay` and `fortune` installed. without `fortune` it uses a canned line;
without `cowsay` it just prints a plain note. nothing errors either way.

## revert

close the terminal.
